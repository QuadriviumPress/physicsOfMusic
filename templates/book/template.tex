%% Print edition of *Physics of Music*.
%%
%% A local jtex template, referenced from myst.yml as `template: ./templates/book`.
%% It began as MyST's stock `plain_latex_book` and diverges from it in five ways,
%% each of which is load-bearing:
%%
%%   1. The problem-set commands that plugins/export.mjs brackets every exercise
%%      and solution with. Neither may be boxed -- see the comment there.
%%   2. Sectioning depth pinned to match the website's numbering exactly.
%%   3. Page geometry and running heads for something meant to be printed.
%%   4. A copyright page and a preface in \frontmatter, because index.md is not
%%      part of the printed book and the preface must stay unnumbered.
%%   5. The `chapter` option, which turns the same source into a standalone
%%      single-chapter offprint numbered as it is in the full book.
%%
%% Built with XeLaTeX (MyST's default: `latexmk -f -xelatex`), which is what lets
%% the raw Unicode in the source -- Å, β, °, ×, ⊕, and `90°` inside math -- pass
%% through without a preamble fight.

[# if options.chapter #]
%% One chapter, standalone. `article` rather than `book`: a one-article export
%% emits no title heading, so the chapter's `##` reading sections arrive as
%% `\section` and there is no `\chapter` left to host. The chapter number is
%% restored to the counters further down.
\documentclass[letterpaper,11pt]{article}
[# else #]
%% oneside/openany, and the margins below, are for a student printing the book
%% single-sided on a classroom printer: no blank verso pages, no gutter offset,
%% and a measure of about 6.25in -- roughly 75 characters, which is what a
%% textbook wants.
\documentclass[letterpaper,11pt,oneside,openany]{book}
[# endif #]

\usepackage[letterpaper,top=1in,bottom=1in,left=1.15in,right=1.15in,
            headheight=14pt]{geometry}

%% XeLaTeX, so fontspec rather than the pdfTeX pair fontenc+inputenc: under T1
%% the 8-bit EC fonts have no slot for the Unicode the source is written in, and
%% Skłodowska, the en dashes, and the ℓ of ℓ-independent degeneracy all come out
%% as silent "Missing character" warnings and blank space on the page.
\usepackage{fontspec}
%% Latin Modern ships its small caps as a separate face; without naming it,
%% \textsc silently falls back to upright and the title page loses its styling.
\setmainfont{Latin Modern Roman}[SmallCapsFont={Latin Modern Roman Caps}]
\setsansfont{Latin Modern Sans}
\setmonofont{Latin Modern Mono}

%% U+2113 SCRIPT SMALL L is an orbital quantum number wherever it appears in
%% this book, and no text font carries it; route it to the math symbol it means.
\usepackage{newunicodechar}
\newunicodechar{ℓ}{\ensuremath{\ell}}

\usepackage{microtype}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{caption}
%% Captions in smaller type: 138 of them carry a full attribution sentence, and
%% at body size the taller ones push a float past the height of the page.
\captionsetup{font=small,labelfont=bf,skip=6pt}
\usepackage{booktabs}
\usepackage{natbib}
\usepackage{xcolor}
\usepackage{changepage}
\usepackage{framed}
\usepackage{float}
\usepackage{needspace}
\usepackage{fancyhdr}
\usepackage{hyperref}
\bibliographystyle{abbrvnat}

% Make list items more compact
\usepackage{enumitem}
\setlist[itemize]{noitemsep, topsep=0pt}

[- IMPORTS -]

%% ---------------------------------------------------------------------------
%% Numbering
%%
%% The website numbers chapters and `##` reading sections (4, 4.1, 4.2 ...) and
%% leaves everything deeper unnumbered; `outline_maxdepth: 2` in myst.yml keeps
%% its outline to the same two levels. These counters are the LaTeX spelling of
%% exactly that, so "Section 4.2" means the same thing in every edition. Without
%% them LaTeX would number the `###` headings too, and "Learning Objectives"
%% would come out as 4.0.1.
%% ---------------------------------------------------------------------------
\setcounter{secnumdepth}{1}
\setcounter{tocdepth}{1}
[# if options.chapter #]
% A one-article export carries no title heading, so the `##` reading sections
% arrive as `\section` and everything below shifts up with them. Prefixing the
% chapter number by hand is what makes an offprint's 4.1, Figure 4.3 and
% equation (4.47) the same 4.1, Figure 4.3 and (4.47) as the full book's --
% which matters, because the prose refers to them by number.
\renewcommand{\thesection}{[-options.chapter-].\arabic{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thefigure}{[-options.chapter-].\arabic{figure}}
\renewcommand{\thetable}{[-options.chapter-].\arabic{table}}
\renewcommand{\theequation}{[-options.chapter-].\arabic{equation}}
[# endif #]

%% ---------------------------------------------------------------------------
%% Palette -- the colors the figures are drawn in (scripts/figures/figstyle.py).
%% ---------------------------------------------------------------------------
\definecolor{mpblue}{HTML}{1769AA}
\definecolor{mpred}{HTML}{B33A3A}
\definecolor{mpgray}{HTML}{555555}
\definecolor{shadecolor}{HTML}{F2F5F8}
\definecolor{darkblue}{rgb}{0.0, 0.0, 0.55}
\definecolor{quoteshade}{rgb}{0.95, 0.95, 1}

%% Links stay legible in black-and-white print but are still visibly clickable
%% on screen, which matters: every simulation reaches its live version through
%% one of these.
\hypersetup{
  colorlinks,
  linkcolor={mpblue},
  citecolor={mpblue},
  urlcolor={mpblue},
  bookmarksnumbered=true,
  bookmarksopen=true,
  pdftitle={[-doc.title-]},
  pdfauthor={[# for author in doc.authors #][-author.name-][# if not loop.last #], [# endif #][# endfor #]}
}

%% ---------------------------------------------------------------------------
%% Running heads
%% ---------------------------------------------------------------------------
\pagestyle{fancy}
\fancyhf{}
[# if options.chapter #]
\fancyhead[L]{\small Physics of Music \textperiodcentered{} Chapter [-options.chapter-]. [-doc.title-]}
[# else #]
\fancyhead[L]{\small\nouppercase\leftmark}
[# endif #]
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\fancypagestyle{plain}{\fancyhf{}\renewcommand{\headrulewidth}{0pt}\fancyfoot[C]{\small\thepage}}

%% ---------------------------------------------------------------------------
%% Problem sets
%%
%% plugins/export.mjs renders every {exercise} and {solution} as a bold run-in
%% title followed by the body, bracketed by the commands below. Two constraints
%% shaped that:
%%
%%   - No box. About a quarter of the worked solutions contain a {figure}, and
%%     LaTeX refuses to open a float inside framed, minipage, or any other box:
%%     it fails with "Not in outer par mode" and the figure, its caption, and
%%     every {numref} pointing at it are lost.
%%   - No custom environment. The same .tex feeds pandoc for the DOCX edition,
%%     and pandoc discards the entire contents of any environment it does not
%%     know. Bare commands it cannot read are skipped harmlessly instead, so the
%%     text between them survives into Word.
%%
%% \leftskip rather than a list: floats escape a group unharmed.
%% ---------------------------------------------------------------------------
\newcommand{\mystexercisestart}{\par\needspace{3\baselineskip}\medskip\noindent}
\newcommand{\mystexerciseend}{\par\medskip}
\newcommand{\mystsolutionstart}{\par\needspace{3\baselineskip}\smallskip
  \begingroup\leftskip=1.2em\relax\noindent}
\newcommand{\mystsolutionend}{\par\endgroup\medskip}

%% Quotes -- and, by way of plugins/export.mjs, {margin} notes. The web theme
%% floats a margin note beside the text; a printed page this wide has no margin
%% to float into, so it becomes a rule-and-tint aside in the flow. pandoc reads
%% \begin{quote} natively, so it survives into Word as a block quotation.
\renewenvironment{quote}{%
  \def\FrameCommand{%
    \hspace{1pt}%
    {\color{darkblue}\vrule width 2pt}%
    {\color{quoteshade}\vrule width 4pt}%
    \colorbox{quoteshade}%
  }%
  \MakeFramed {\advance\hsize-\width \FrameRestore}%
  \noindent\hspace{-8pt}% disable indenting first paragraph
  \begin{adjustwidth}{0pt}{0pt}% adjust as needed
  \vspace{2pt}\vspace{2pt}%
}
{%
  \vspace{2pt}\end{adjustwidth}\endMakeFramed%
}

\title{\Huge \textbf{[-doc.title-]}[# if doc.subtitle #] \\[0.5em] \huge [-doc.subtitle-][# endif #]}
\author{\textsc{
[#- for author in doc.authors #]
  [#- if not loop.first #] and [# endif #][-author.name-][# if not loop.last and not loop.first #], [# endif #]
[#- endfor -#]
}}
[# if doc.date #]\date{[-doc.date-]}[# endif #]

\begin{document}
\sloppy

[# if options.chapter #]
%% A single chapter gets a masthead rather than a title page: it is an offprint,
%% and it should say so and say where the rest of the book lives.
\thispagestyle{plain}
\begin{center}
  {\small\scshape\color{mpgray} Physics of Music}\\[0.35em]
  {\LARGE\bfseries Chapter [-options.chapter-]. [-doc.title-]}\\[0.6em]
  {\small An offprint of one chapter of the open textbook, by
   [# for author in doc.authors #][-author.name-][# if not loop.last #], [# endif #][# endfor #]}\\[0.2em]
  {\small\color{mpgray} CC\--BY\--NC\--SA\--4.0 \textperiodcentered{}
   \url{https://github.com/QuadriviumPress/physicsOfMusic}}
\end{center}
\vspace{1em}
\hrule
\vspace{1.5em}

[- CONTENT -]
[# else #]
\frontmatter
\maketitle

%% The copyright page. index.md carries this on the website, but index.md is a
%% landing page -- a "Contents" widget and a licence notice -- and is not one of
%% the printed book's articles, so the notice lives here instead. Keep it in step
%% with index.md and with `license:` in myst.yml.
\thispagestyle{empty}
\null\vfill
\noindent\small
\textcopyright{} 2026 Martin Veillette.\par\medskip
\noindent This book is licensed under the Creative Commons
Attribution\--NonCommercial\--ShareAlike 4.0 International License
(CC\--BY\--NC\--SA\--4.0). You are free to share and adapt it for
non-commercial purposes, with attribution, under the same licence:\par\smallskip
\noindent\url{https://creativecommons.org/licenses/by-nc-sa/4.0/}\par\medskip
\noindent This book adapts openly licensed content from several sources. The
per-chapter attribution ledger is \texttt{SOURCES.md}:\par\smallskip
\noindent\url{https://github.com/QuadriviumPress/physicsOfMusic/blob/main/SOURCES.md}
\par\medskip
\noindent The interactive simulations reproduced here as screenshots are
published by PhET Interactive Simulations (University of Colorado Boulder) and
by the OpenLyceum project under their own licences; each caption links to the
running simulation.
\par\vfill\normalsize

\tableofcontents

%% Stay in \frontmatter through the preface (the first article). In book class a
%% \chapter issued here is unnumbered but still enters the TOC, which is what a
%% preface wants. preface.md ends with `{raw:latex}\mainmatter`, so Chapter 1 is
%% the first numbered chapter and the part dividers land in \mainmatter.
[- CONTENT -]
[# endif #]

[# if doc.bibliography #]
\bibliography{[- doc.bibliography | join(", ") -]}
[# endif #]

\end{document}
